#include "navstatemachine.h"

int main(int argc, char **argv)
{
  NavStateMachine navStateMachine(argc, argv);

  /*if(!moveManager.initConn())
  {
    moveManager.rotateAndMove(100,100);
    moveManager.rotateAndMove(0,-100);
  }
  moveManager.closeConn();*/
  return 0;
}